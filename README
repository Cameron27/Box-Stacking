Program usage is:
java NPStack [file] [interations]

Solution used simulated annealing where to being multiple random stacks are generated and the best one is picked to be the starting point. Then, for each allowed consideration
a number of random changes are made to the stack of boxes. The possible changes are removing a box, replacing a box or inserting a box. After all the random changes, if the
new stack is better than the previous stack it will replace it.